apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/jstrachan/phpdemo7:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/jstrachan/phpdemo7:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: phpdemo7
        chartPath: charts/phpdemo7
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/jstrachan/phpdemo7'
          image.tag: '{{.DIGEST_HEX}}'
